{
  "schemaVersion": 1,
  "defaultProviderOrder": ["claude", "codex"],
  "providers": {
    "codex": {
      "command": "codex",
      "args": ["exec", "-s", "read-only", "-C", "{{rootDir}}", "-o", "{{lastMessageFile}}", "-"],
      "timeoutMs": 600000
    },
    "claude": {
      "command": "claude",
      "args": ["-p", "--permission-mode", "dontAsk", "--output-format", "json", "--debug-file", "{{debugFile}}"],
      "timeoutMs": 600000
    }
  }
}
